# Perintah untuk disalin dan dijalankan di VM WEB SERVER

apt update
apt install nginx -y
apt-get install -y php8.2-fpm php8.2-cli php8.2-curl php8.2-zip php8.2-gd
apt-get install -y php8.2-xml php8.2-intl php8.2-mbstring php8.2-xmlrpc php8.2-soap
apt-get install -y php8.2-bcmath php8.2-exif php8.2-ldap php8.2-mysql unzip
wget https://packaging.moodle.org/stable500/moodle-latest-500.zip
unzip moodle-latest-500.zip -d /var/www/
rm moodle-latest-500.zip

# Perintah untuk menghapus file txt ini dan file server.txt (jika ada)
rm -f web.txt
