# Perintah untuk disalin dan dijalankan di VM DATABASE SERVER

apt update
apt install mariadb-server mariadb-client -y

# Perintah untuk menghapus file txt ini dan file web.txt (jika ada)
rm -f server.txt